const a = 2 + 3; //=> 5
const msg = "hi " + "there"; //=> "hi there"
const arr = [1, 2, 3].map((x) => x * 2); //=> [ 2, 4, 6 ]
console.log(msg, a); //=> "hi there" 5
const obj = { name: "Ada", tags: ["math"] }; //=> { name: "Ada", tags: [ "math" ] }
