// Imported by eval/cases/imports.ts (Phase 6 project-import resolution).
// Lives outside eval/cases/ so the harness doesn't run it as a standalone case.
export const double = (x: number): number => x * 2;
